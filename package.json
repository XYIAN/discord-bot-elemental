{
  "name": "discord-bot-elemental",
  "version": "1.0.4",
  "description": "Game-specific Discord bot template for community knowledge and progression",
  "private": true,
  "scripts": {
    "start": "node bot.js",
    "dev": "node bot.js",
    "setup:roles": "node scripts/create-roles.js",
    "setup:channels": "node scripts/create-channels.js",
    "setup:permissions": "node scripts/apply-permissions.js",
    "setup:welcome": "node scripts/seed-welcome-posts.js",
    "setup:dry-run": "node scripts/create-roles.js --dry-run && node scripts/create-channels.js --dry-run && node scripts/apply-permissions.js --dry-run && node scripts/seed-welcome-posts.js --dry-run",
    "setup:all": "node scripts/create-roles.js && node scripts/create-channels.js && node scripts/apply-permissions.js && node scripts/seed-welcome-posts.js",
    "audit:ai": "node scripts/audit-elemental-ai.js",
    "backup:data": "node scripts/backup-data.js"
  },
  "author": "XYIAN",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^6.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
